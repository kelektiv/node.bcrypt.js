name: test

on: [push, pull_request]

jobs:
  build:

    runs-on: [windows-2019, ubuntu-18.04, macos-11.0, macos-10.15]

    steps:
    - uses: actions/checkout@v2
    - name: make test
      run: make test
